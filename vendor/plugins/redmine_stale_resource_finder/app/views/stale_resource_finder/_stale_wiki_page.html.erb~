<li>
	Page: <%= link_to(stale_wiki_page.title, :controller => "wiki", :action => "index", :id => stale_wiki_page.wiki.project, :page => stale_wiki_page.title) %><br/>
	<% stale_wiki_page.content.stale_embedded_images.each do |stale_image| %>
	  Resource url: <strong><%= stale_image.path %></strong><br/>
	  Currently in wiki: <%= link_to(stale_image.embedded_version, stale_image.path_to_version(stale_image.embedded_version)) %> 
	  	<small>committed by</small> <%= stale_image.revision_info_for(stale_image.embedded_version).author %> 
		<small>on</small> <%= stale_image.revision_info_for(stale_image.embedded_version).time %><br/>
		Commit message: <%= stale_image.revision_info_for(stale_image.embedded_version).message %>
		<p><%= image_tag(stale_image.resource_absolute_path + "?rev=#{stale_image.embedded_version}", :width => "300x300") %></p>	  
		
	  Latest in repo: <%= link_to(stale_image.repository_version, stale_image.path_to_version(stale_image.repository_version)) %>
	  	<small>committed by</small> <%= stale_image.revision_info_for(stale_image.repository_version).author %> 
		<small>on</small> <%= stale_image.revision_info_for(stale_image.repository_version).time %><br/>
		Commit message: <%= stale_image.revision_info_for(stale_image.repository_version).message %>		
		<p><%= image_tag(stale_image.resource_absolute_path + "?rev=#{stale_image.repository_version}", :width => "300x300") %></p>	  
	<% end %>
</li>